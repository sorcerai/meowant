[pytest]
# Collect only the real suite. Keeps ad-hoc scratch test_*.py in the repo root
# (device probes, gemma experiments) from colliding with tests/ on basename and
# breaking collection.
testpaths = tests

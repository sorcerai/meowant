# Secrets — contains local_key and Tuya cloud credentials. NEVER commit.
config.json

# Scratch directory (untracked utilities, logs, backups, secrets)
scratch/

# Python
__pycache__/
*.pyc
.venv/
venv/

# tinytuya artifacts
devices.json
snapshot.json
tuya-raw.json
tinytuya.json

# Logs and backups
*.log
*.bak-*
cycle_log.tsv
*.tsv

# cat photos / training data (privacy + size)
gallery/

# SQLite databases
*.db

# Agent / tooling state — LOCAL ONLY, never publish (whole dirs)
.beads/
.dolt/
.beads-credential-key
.claude/
.codex/
.agents/
.superpowers/
.mcp.json

# macOS + scratch
.DS_Store
council_err.log

# daemon log
meowantd.log

# local-only non-technical explainer (kept out of the repo)
docs/explainer/
deadman_state.json
deadman.log
weekly_state.json

# Frontend
frontend/node_modules/
frontend/dist/
warm_frames/

# DINOv2 gallery artifacts (regenerate: PYTHONPATH=. python3 scripts/build_gallery.py)
gallery.npz
gallery_emb_cache.npz
